# Proxmox API token format: user@realm!tokenid=secret
# Keep both variables aligned if you define both.
TF_VAR_proxmox_api_token=terraform@pve!pipeline-token=replace-with-real-secret
PROXMOX_VE_API_TOKEN=terraform@pve!pipeline-token=replace-with-real-secret
